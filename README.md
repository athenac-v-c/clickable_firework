# Clickable Firework Demo
 Click anywhere on the screen to spawn colorful fireworks.

## How to Run
1. Start the game:
   ```
   sh run.sh
   ```
2. Remove the virtual environment and Python cache files:
   ``` 
   sh clean.sh
   ```

